# -------------------------------------------------
# ป้องกันการรันสคริปต์ในโฟลเดอร์ Uploads
# -------------------------------------------------

# (วิธีที่ 1: สำหรับ Apache/LiteSpeed ส่วนใหญ่)
# ปิดการรันสคริปต์ทุกชนิด
<FilesMatch "\.(php|php[3-8]|phtml|phar|inc)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# (วิธีที่ 2: ถ้าวิธีแรกไม่ได้ผล)
# บอกให้เซิร์ฟเวอร์จัดการไฟล์ .php เหมือนเป็น Text ธรรมดา
<Files *.php>
    ForceType text/plain
</Files>